import Foundation

enum SystemSettingsIdentifiers {
    static let bundleIdentifier = "com.apple.systempreferences"
    static let applicationURL = URL(fileURLWithPath: "/System/Applications/System Settings.app")
}
